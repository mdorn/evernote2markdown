============
Installation
============

At the command line::

    $ easy_install evernote2markdown

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv evernote2markdown
    $ pip install evernote2markdown
